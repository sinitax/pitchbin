services:
  pitchbin:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    volumes:
      - data:/data
    ports:
      - ${PITCHBIN_BIND:-8080}:8080
    command:
      - -addr
      - ":8080"
      - -db
      - /data/pitchbin.db
      - -base-url
      - ${PITCHBIN_BASE_URL:-https://pitchbin.xyz}
      - -pow-bits
      - ${PITCHBIN_POW_BITS:-18}
    restart: unless-stopped

volumes:
  data:
